# История итераций

Формат: что изменилось → чем проверено. Правки модели без прогона регресса сюда не попадают.

## 2026-08-10, вечер

### Модель: честная стопа на педали

Расчётная высота седла не сходилась с фактической. Разбор показал две дыры и одну подмену понятий.

**Платформы педали в модели не было вовсе.** От оси педали до подошвы 5–15 мм, и они поднимают седло 1:1. Добавлено поле `pedalStack` (замерено на реальной педали: 7 мм).

**`soleThk` понималась как «толщина подошвы» вообще.** Подошва почти всегда клиновидная — у сланца с фото 28 мм у пятки и 17 у носка. Теперь это толщина **в точке контакта**, там, где под ней ось педали.

**`ankleH` работал подгоночным коэффициентом.** Не имея двух полей выше, его раздували сверх анатомии: было 105 мм при анатомических 73 — на 40% выше нормы. Теперь на это срабатывает проверка правдоподобия, а сам параметр вернулся к своей роли.

Итог проверки на реальном велосипеде (Warhog 140, замеренная высота седла 705 мм):

| | |
|---|---|
| платформа педали над осью | 7 (замерено) |
| подошва в точке контакта | 24 (клин 28→17, педаль под сводом) |
| голеностоп над стелькой | 76 (анатомия: 0.039 × 1870 = 73) |
| **итого над осью** | **108** |
| расчётная высота седла | **705.9** |
| фактическая | **705** |

Сходится в пределах миллиметра, причём средний слой (76) не подгонялся, а получился близко к антропометрической доле — это независимая проверка. Кросс-сверка на второй раме с тем же шатуном 175: 707.6 мм.

**Добавлен наклон подошвы `soleTilt`** (+ носок вниз). Голеностопа как сустава по-прежнему нет, подошва держит один угол на всём обороте, но угол стал явным вместо жёсткого нуля. При зафиксированном седле 15° носком вниз меняют угол колена в НМТ со 143° до 135°, 15° пяткой вниз — до 159°. Ровно это видно между двумя фото одной посадки с разной постановкой стопы.

**Добавлено `pedalPos`** — где ось педали вдоль стопы, от пятки. Вопреки ожиданию, на высоту седла почти не влияет: сдвиг на 84 мм двигает седло на 7 мм, потому что нога в НМТ почти вертикальна и смещение уходит в косинус. Нужно для рисунка и продольного положения колена.

Фикстура обновлена под перезамеренные данные (торс 660 → 610 после перепроверки, hipAbove 64 → 34, hipFwd −24 → −34, kneeTarget 143, spineFlex 12, ролл руля на трёх кокпитах, upsweep на Warhog). Эталон переснят.

## 2026-08-10

### Инфраструктура: регресс между сессиями

Добавлены `regress.mjs` и зафиксированный `regress-golden.json`. До этого эталон снимался с текущего кода в начале каждой сессии, поэтому регрессия, заехавшая раньше, молча становилась частью нового снимка — все проверки «бит в бит» были верны только внутри одного разговора.

108 кейсов, 5292 значения: три реальные связки рама+кокпит из `fixtures-evo.json` на сетке sag × наклон корпуса × разведение × наклон велосипеда. Зависимостей нет, модель вынимается из `fit-lab.html` и исполняется в заглушке DOM.

Проверено: искусственно убрал доворот руля под sag — скрипт поймал 1890 расхождений в 35 параметрах и вернул ненулевой код. После восстановления прогон чист.

Добавлены `CLAUDE.md` (рабочее соглашение, читается любым чатом автоматически) и этот файл. `README.md` сокращён до обзора, описание модели целиком переехало в `FIT-MODEL.md`, инструкция пользователя живёт в `help.html`.

### UX: сводка и порядок блоков

Порядок на странице переставлен под цикл правки: вид сбоку → сводка → фото-калибровка → вид сверху → стойка со своей таблицей → полная таблица.

Новая карточка «Сводка» — шесть блоков плитками с числами, на которые смотришь каждую итерацию: подъём выноса, прямая каретка → седло, вылет с эффективным reach, RAD с целевым и RAAD, наклон корпуса с запасом руки, кисти с балансом. Ничего не считает сама, только собирает наверх.

Блок стойки вырезан из общей таблицы (35 строк) и живёт под своей схемой.

Проверено: модель не тронута, выходы бит в бит.

### Модель: аудит цепочки руки

**Общая функция `elbow3D` на обе позы.** Посадка и стойка считали руку по-разному; теперь одна функция — 3D-расстояние, протракция, орбита локтя, оба угла, отведение, вынос. Перевод посадки дал бит в бит. Анатомический угол из `elbow3D` совпал с целевым углом стойки до сотых при любом наклоне и разведении.

**Убран второй, фальшивый локоть.** В модели их было два: плоская конструкция `arm.J` шла в отрисовку и фото-калибровку и к разведению была нечувствительна вообще, настоящий `E3` — в предплечье и залом запястья. Их проекции расходились до 143 мм. Теперь везде проекция `E3`, включая центр масс.

**Два угла локтя вместо одного.** Анатомический (в плоскости руки) и в профиль (то, что видно сбоку и на фото). Разводит их разведение локтей: до 30° разница в пределах 2°, при 60° уже 18°, при 90° — 50°. Фото-калибровка сверяла отмеченный на боковом снимке угол с анатомическим — систематическая ошибка до 50°, исправлено.

**Разведение обрело физический смысл.** Градусы обманчивы: орбита локтя схлопывается по мере разгиба, и одни и те же 30° дают 105 мм выноса при наклоне 58° и 67 мм при 71°. Под слайдером выводится вынос в мм, отведение плеча и доля использованного диапазона. Предупреждение при отведении выше 45–50° — зона импинджмента.

Не сделано осознанно: связь разведения с высотой плеча. Кинематически невозможно при зафиксированных тазе и ручках; ощущение «развожу локти и опускаю плечи» реально, но механизм скапулоторакальный, и калибровать его нечем.

Регресс: изменился только `frontPct`, максимум на 0.012 п.п. — уточнение центра масс.

### Модель: руль доворачивается вместе с рамой

Вектор руля собирался в мировых осях, поэтому под просадкой вилки вынос наклонялся вместе с рамой, а руль оставался по уровню — вёл себя как подвешенный в кардане. `solveFrame` теперь отдаёт `theta` и `pitchSag`, `solveCockpit` крутит руль на `barRoll + pitchSag`. Семантика `barRoll` зафиксирована: угол задаётся **на стоящем велосипеде**, по уровню.

Заодно починена ветка спуска в стойке: ось грипсы теперь вращается вместе с велосипедом, а не остаётся исходной. При наклоне до 45° ошибка оси была бы 45°.

Побочный результат — инвариант: **RAD перестал зависеть от sag** (разброс 2·10⁻¹³ против прежних 2.5 мм). Каретка и ручка обе жёстко на раме, расстояние между ними физически не может зависеть от наклона. Записан в `CLAUDE.md` как сторож.

Величины небольшие: наклон рамы 0.87° (ход 100, sag 20%) — 3.18° (ход 180, sag 40%), сдвиг ручки 0.8–2.9 мм.

### Модель: два числа высоты седла, LeMond убран

Выводятся ход вдоль оси подседельной трубы и прямая каретка → седло. Совпадают только при нулевом сетбеке: офсетный штырь разводит их на 6–13 мм, а рулеткой меряют именно прямую. LeMond убран из таблицы и документации — формула не знает про длину шатуна и расходилась с моделью тем сильнее, чем ниже райдер.

### Модель: подошва и 3D-дистанция до ручки

**Толщина подошвы работала вхолостую.** Она попадала и в отступ лодыжки над осью педали (верно), и в вычитание из длины ноги (неверно — inseam меряется босиком). Эффекты гасили друг друга: 45 мм подошвы двигали седло на 2 мм вместо 45. Теперь 1:1.

Побочно улучшилось согласие с LeMond — расхождение сократилось с 30 до 12 мм, отдельная подгонка `hipAbove` перестала быть нужной.

**Расстояние плечо → ручка считается в 3D.** Раньше рука решалась в плоскости велосипеда, хотя ручка отстоит вбок; занижение 8.5 мм. Порог «руль недостижим» стал срабатывать честнее.

### Раскладка страниц

Обе страницы перестроены в две панели: ввод слева, результат справа. Три точки перестроения — 700 и 1160 px, ширина панели `clamp(330px, 25%, 500px)`. Высота схем ограничена долей высоты окна с учётом пропорций viewBox, чтобы вид сбоку помещался целиком рядом с полями.

### Кокпит: RAD, эффективные reach и stack

Добавлены RAD (прямая каретка → центр хвата), RAAD, целевой RAD от роста и эффективные reach/stack — то же, но по осям. Множитель проверен на собственном примере Маккормака: 174 см → 778 мм.
